package no.nav.hjelpemidler.soknad.mottak.river

import com.fasterxml.jackson.module.kotlin.convertValue
import com.github.navikt.tbd_libs.rapids_and_rivers.JsonMessage
import com.github.navikt.tbd_libs.rapids_and_rivers.River
import com.github.navikt.tbd_libs.rapids_and_rivers_api.MessageContext
import com.github.navikt.tbd_libs.rapids_and_rivers_api.RapidsConnection
import io.github.oshai.kotlinlogging.KotlinLogging
import no.nav.hjelpemidler.behovsmeldingsmodell.BehovsmeldingStatus
import no.nav.hjelpemidler.behovsmeldingsmodell.Behovsmeldingsgrunnlag
import no.nav.hjelpemidler.logging.teamInfo
import no.nav.hjelpemidler.serialization.jackson.jsonMapper
import no.nav.hjelpemidler.soknad.mottak.melding.BehovsmeldingTilGodkjenningMelding
import no.nav.hjelpemidler.soknad.mottak.metrics.Metrics
import no.nav.hjelpemidler.soknad.mottak.metrics.Prometheus
import no.nav.hjelpemidler.soknad.mottak.soknadsbehandling.SøknadsbehandlingService
import java.util.UUID
import no.nav.hjelpemidler.configuration.Environment

private val log = KotlinLogging.logger {}

/**
 * Plukker opp behovsmeldinger som må bekreftes av bruker (i hm-dinehjelpemidler på Mitt NAV) før de kan sendes videre i flyten.
 */
class BehovsmeldingTilBrukerbekreftelseDataSink(
    rapidsConnection: RapidsConnection,
    private val søknadsbehandlingService: SøknadsbehandlingService,
    private val metrics: Metrics,
) : AsyncPacketListener {
    init {
        River(rapidsConnection).apply {
            precondition {
                it.requireValue("eventName", "nySoknad")
                it.requireValue("signatur", "BRUKER_BEKREFTER")
            }
            validate {
                it.requireKey("fodselNrBruker", "fodselNrInnsender", "soknad", "eventId", "behovsmelding")
                it.forbid("soknadId")
            }
        }.register(this)
    }

    private val JsonMessage.eventId get() = uuidValue("eventId")
    private val JsonMessage.søknadId get() = this["soknad"]["soknad"]["id"].uuidValue()
    private val JsonMessage.søknad get() = this["soknad"]
    private val JsonMessage.fnrInnsender get() = this["fodselNrInnsender"].textValue()
    private val JsonMessage.fnrBruker get() = this["fodselNrBruker"].textValue()
    private val JsonMessage.navnBruker get() = this["soknad"]["soknad"]["bruker"]["fornavn"].textValue() + " " + this["soknad"]["soknad"]["bruker"]["etternavn"].textValue()
    private val JsonMessage.behovsmeldingV2 get() = this["behovsmelding"]

    override suspend fun onPacketAsync(packet: JsonMessage, context: MessageContext) {
        if (packet.eventId in skipList) {
            log.info { "Hopper over event i skipList: ${packet.eventId}" }
            return
        }

        val søknadId = packet.søknadId
        val fnrBruker = packet.fnrBruker

        try {
            val søknad = søknadsbehandlingService.finnSøknad(søknadId)
            if (søknad != null) {
                log.warn { "Søknaden er allerede lagret i databasen: $søknadId" }
                return
            }

            val behovsmeldingGjelder = if (Environment.current.isDev) {
                // dev: prøv å lese feltet direkte fra søknaden dersom det finnes
                log.info {
                    "Prøver å bruke nytt felt for behovsmelding gjelder, verdi: ${packet.søknad["behovsmeldingGjelder"]?.textValue()}"
                }
                packet.søknad["behovsmeldingGjelder"]?.textValue()
                    ?: AutoGenerateDocumentTitle.generateTitle(packet.søknad)
            } else {
                // prod: bruk gammel tittel-generering for sikkerhets skyld (gamle søknader har ikke felter nødvendige for å bruke nye feltet)
                AutoGenerateDocumentTitle.generateTitle(packet.søknad)
            }

            val grunnlag = Behovsmeldingsgrunnlag.Digital(
                søknadId = søknadId,
                status = BehovsmeldingStatus.VENTER_GODKJENNING,
                fnrBruker = fnrBruker,
                // Lagres til hm-soknadsbehandling-db, slik at det kan vises i hm-formidler, selv om bruker sletter søknaden (brukerbekreftelse)
                navnBruker = packet.navnBruker,
                fnrInnsender = packet.fnrInnsender,
                behovsmelding = jsonMapper.convertValue(packet.søknad),
                behovsmeldingGjelder = behovsmeldingGjelder,
                behovsmeldingV2 = jsonMapper.convertValue(packet.behovsmeldingV2),
            )

            log.info { "Behovsmelding til godkjenning mottatt, søknadId: $søknadId (gjelder: '${grunnlag.behovsmeldingGjelder}')" }

            søknadsbehandlingService.lagreBehovsmelding(grunnlag)

            context.publish(fnrBruker, BehovsmeldingTilGodkjenningMelding(grunnlag))
            Prometheus.søknadTilGodkjenningCounter.increment()
            log.info { "Behovsmelding klar til godkjenning, søknadId: $søknadId" }
            log.teamInfo { "Behovsmelding klar til godkjenning, søknadId: $søknadId, fnrBruker: $fnrBruker" }
            metrics.digitalSøknad(fnrBruker, søknadId)
        } catch (e: Exception) {
            log.error(e) { "Håndtering av eventId: ${packet.eventId}, søknadId: $søknadId feilet" }
            throw e
        }
    }

    private val skipList = listOf<UUID>()
}
